version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - rvm install ruby-2.5.3
        - rvm use 2.5.3
        - bundle install
    build:
      commands:
        - bundle exec jekyll b
  artifacts:
    baseDirectory: _site
    files:
      - '**/*'
  cache:
    paths: []
